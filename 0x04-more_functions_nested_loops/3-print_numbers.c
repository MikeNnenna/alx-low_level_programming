#include "main"

/**
 * print_numbers - prints numbers
 * main - entry point 
 * Return: 0 always
 */

void print_numbers(void)
{
	int x;

	x = 0
	while (x <= 9)
	{
		_putchar(x);
		x++
	}
	_putchar('\n');
	return (0);
}
