#include "main.h"

/**
 * _isupper - checks for uppercase
 * main - entry point
 * Return: 1 if successful || 0 if otherwise
 */

int _isupper(int c)
{
	for (x = A, x <= Z, x++)
	{
		if (x = C)
		{
			printf("x\n");
		return (1);
		}
		else 
		{
			return (0);
		}
	}
}
