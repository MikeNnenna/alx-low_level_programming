#include "main.h"

/**
 * print_alphabet_x10 - prints the alphabets
 * main - uses inbuilt function
 * Return: 0 if success
 */

void print_alphabet_x10(void)
{
	int x;
	char i;
	x = 0;

	while (x <= 0)
	{
		for (i = 'a'; i <= 'z'; i++)
			_putchar(i);
		_putchar('\n');
	}
	x++
}
