#include "main.h"

/**
 * print_alphabet - prints the alphabets
 * Return: 0 if success
 */

void print_alphabet(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
		_putchar('a');
	}
	_putchar('\n');
	return (0);
}
