#include <stdio.h>
/**
 * main - entry point 
 * Description: starting point of code
 * Return: 0 if success
 */
int main(void)
{
	printf("size of char: %d byte(s)\n", size of (char));
	printf("size of int: %d byte(s) \n", size of (int));
	printf("size of long int: %d byte(s\n", size of (long int));
	printf("size of long long int: %d byte(s)", size of (long long int));
	peintf("size of float: %d byte(s)", size of (float));
	return (0);

