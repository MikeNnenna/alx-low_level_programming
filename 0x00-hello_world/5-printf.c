#include <stdio.h>
/**
 * main - Starting point of the program
 * Description: enty point of the program
 * Return: always 0 if success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
